# ArkUI-X Android 快速参考卡

## 🚀 快速命令

```bash
# 新项目初始化
script\setup-android.bat

# 检查配置
script\check-config.bat

# 一键编译运行
script\android\build-and-run.bat
```

## ⚠️ 关键配置

### HarmonyOS SDK 版本
```json5
// ohos/build-profile.json5
"compileSdkVersion": 12,  // 必须是 12！
"compatibleSdkVersion": 12
```

### Android jniLibs
```gradle
// android/app/build.gradle
sourceSets {
    main {
        jniLibs.srcDirs = ['libs']  // 关键配置
    }
}
```

### Activity 实例名
```java
// EntryEntryAbilityActivity.java
setInstanceName("com.example.hmrdp:entry:EntryAbility:");  // 注意最后的冒号
```

## 📁 关键文件位置

| 文件 | 位置 | 说明 |
|------|------|------|
| 插件库 | `android/app/libs/arm64-v8a/*.so` | 69 个文件，必需！ |
| 字节码 | `assets/arkui-x/entry/ets/modules.abc` | 从 HarmonyOS 编译输出同步 |
| 系统资源 | `assets/arkui-x/systemres/` | 从 SDK 复制 |

## 🔧 常见错误速查

| 错误 | 原因 | 解决 |
|------|------|------|
| `abc file version 13.0.1.0` | SDK 版本错误 | 改为 12 |
| `library ".../libhilog.so" not found` | 插件库缺失 | 运行 `setup-android.bat` |
| `uiContent_ is nullptr` | UI 未渲染 | 检查 modules.abc 和插件库 |
| Activity 不存在 | 包名/类名错误 | 检查 run.bat 配置 |

## 📋 开发流程

### 首次设置
```
1. setup-android.bat          → 初始化
2. check-config.bat            → 验证
3. [DevEco 编译]              → Build Hap(s)
4. build-and-run.bat           → 编译运行
```

### 日常开发
```
1. [修改 ArkTS 代码]
2. [DevEco 重新编译]
3. build-and-run.bat           → 自动同步并运行
```

## 🐛 调试命令

```bash
# 查看 logcat
adb logcat -s "Ace:*" "NAPI:*" "Hmrdp:*"

# 检查 APK 内容
unzip -l app-debug.apk | grep "lib/arm64"

# 获取应用 PID
adb shell pidof com.example.hmrdp
```

## 📚 完整文档

- [ArkUI-X安卓编译参考手册.md](ArkUI-X安卓编译参考手册.md) - 完整手册
- [script/README.md](script/README.md) - 脚本详细说明
- [脚本工具使用说明.md](脚本工具使用说明.md) - 工具总结

---

**版本**: 1.0 | **更新**: 2026-03-23
